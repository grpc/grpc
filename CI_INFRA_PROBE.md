# CI infrastructure probe

Temporary, throwaway file used to trigger a full CI run against an otherwise
unmodified master, to determine whether macOS job failures are caused by
infrastructure rather than by any code change.

This file is not referenced by anything and must not be merged.
