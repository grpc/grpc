type: google.api.Service
config_version: 3
name: testing.googleapis.com

title: gRPC Benchmark API

documentation:
  summary: >
    Benchmarks gRPC client.

apis:
- name: grpc.testing.BenchmarkService

authentication:
  rules:
    - selector: '*'
      oauth:
        canonical_scopes: https://www.googleapis.com/auth/pubsub,
                          https://www.googleapis.com/auth/cloud-platform
