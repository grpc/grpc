#================
# C# dependencies

# Install dependencies
RUN apt-get update && apt-get install -y \
    mono-devel \
    ca-certificates-mono \
    nuget \
    cmake \
    && apt-get clean

RUN nuget update -self
