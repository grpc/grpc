language: objective-c
before_install:
  - brew install gflags
env:
  global:
    - CONFIG=opt
    - TEST=objc
    - JOBS=1
script:
  - ./tools/run_tests/run_tests.py -l $TEST -t -j $JOBS -c $CONFIG -s 4.0
notifications:
  email: false
