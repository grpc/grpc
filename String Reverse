#include <algorithm> 
#include<iostream>
#include<string>
using namespace std; 
void main() 
{ 
    string str = "Hello this is GitHub"; 
    reverse(str.begin(), str.end()); 
    cout<<"\n"<<str; 
   
}
