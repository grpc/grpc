rewrite (github.com)/(.*) https://storage.googleapis.com/grpc-bazel-mirror/$1/$2
rewrite (github.com)/(.*) https://$1/$2
rewrite (sourceforge.net)/(.*)/download https://storage.googleapis.com/grpc-bazel-mirror/$1/$2
rewrite (sourceforge.net)/(.*) https://storage.googleapis.com/grpc-bazel-mirror/$1/$2
rewrite (sourceforge.net)/(.*) https://$1/$2
rewrite (sourceware.org)/(.*) https://storage.googleapis.com/grpc-bazel-mirror/$1/$2
rewrite (sourceware.org)/(.*) https://$1/$2
